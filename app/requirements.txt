# === Python 版本说明 ===
# Intel Mac (x86_64)：PyTorch 2.3 起不再发布 Intel Mac 安装包，
#   本文件锁定的 torch==2.2.2 仅支持 Python ≤3.12 → 必须用 python3.11
# ARM Mac (M1/M2/M3)：无此限制，Python 3.12+ 可直接使用
# 不装语义匹配：Python 3.10+ 任意版本均可
#
# === 基础运行依赖 ===
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.2
openpyxl>=3.1
xlrd>=2.0
# 冒烟测试
httpx>=0.27
# 语义匹配（可选，安装失败不影响运行——系统降级为纯规则模式）
# Intel Mac (x86_64)：torch 新版不再发布 x86_64 mac 轮子，最后可用版本为 2.2.2
# torch 2.2 仅支持 Python ≤3.12 → 必须用 python3.11 创建 venv
torch==2.2.2
numpy<2
sentence-transformers>=3.0,<4
modelscope>=1.20
