# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 操作本仓库提供指引。

## 仓库内容

两部分：(1) 南湾街道（深圳龙岗区）"企业×应用场景"供需匹配项目的 6 张源 Excel 工作簿，
(2) **`app/` — 一个完整的本地 Web 系统（FastAPI + SQLite + BGE 语义匹配）**，在文件 1~5
之上构建，包含政府端 CRUD、匹配工作台、项目看板、审核流程，以及一个可开关的企业端入口。
文件 6（政府采购中标记录）本期不纳入。完整系统文档见 [app/README.md](app/README.md)。

## 系统运行

- `cd app && .venv/bin/python run.py` → http://127.0.0.1:8000
  （首次运行自动创建管理员 admin / nanwan2026，三个角色：admin / gov / enterprise）
- 冒烟测试：`.venv/bin/python scripts/smoke.py`（22 项检查：数据计数、登录、CRUD 页、已知配对回归）
- 重新导入 Excel：`.venv/bin/python -m ingest.excel_import`（CLI 入口，幂等 upsert；匹配**不会**自动重算——需在 UI 上点击「重新计算」）
- 虚拟环境**必须用 Python 3.11**（Intel Mac：torch 2.3 及以上不再发布 x86_64 mac 轮子，且 torch 2.2 需 ≤3.12）。通过清华镜像安装（`-i https://pypi.tuna.tsinghua.edu.cn/simple`）；HuggingFace 本机不可达——模型权重通过 ModelScope 下载，见 `scripts/download_model.py`。

## app/ 常见坑

- starlette 1.x 的 `TemplateResponse(request, name, context)` 是新签名；不要用旧的 `(name, context)` 形式。
- `matches` 表中人工标记状态（已对接/已排除）须在 `engine.recompute_all` 中保留——它只删除「候选」行，其余通过 `INSERT ... ON CONFLICT DO UPDATE` 更新。
- 企业端路由已存在，但在 `ENTERPRISE_PORTAL_ENABLED=False` 时返回"暂未开放"提示（见 [app/config.py](app/config.py)）；部署到服务器后改为 True 即开放。
- 语义层可优雅降级：未安装 torch/模型时，`embed.available()` 返回 False，匹配退化为纯规则模式。

## 匹配引擎（规则 + 语义）

评分公式：`0.7 × BGE 语义余弦 + 0.3 × 规则（0.5 类别映射 + 0.5 领域词命中 / 3）`。完整设计见 [匹配算法设计.md](匹配算法设计.md)。

- **语义路径**：BGE-small-zh-v1.5（ModelScope 下载），非对称编码（场景=query，带指令前缀；企业=passage），归一化向量 → 点积 = 余弦。向量存入 `embeddings` 表并带 `model_ver`。
- **规则路径**：类别桶→领域映射（`matching/rules.py` 的 `CATEGORY_DOMAIN_MAP`）+ 领域词库（19 个领域 × 5~12 词，3 词饱和）。词库词条均为"专属证据"（泛词如 监测/智能/自动化 一律禁入）。英文缩写单例（AI/AR/VR/AGV/MEMS）带 ASCII 词边界守卫。
- **领域词库存于两处**：(1) `matching/rules.py:DOMAIN_LEXICON` — 出厂默认词库，语料挖掘 + 人工审定；(2) `lexicon_terms` 数据库表 — 管理员可通过 `/gov/lexicon` 在线增删词条，有数据时优先于代码默认值，空表 → 回退代码值。修改后下一次重算生效，**无需重启服务**。文件 4 中的复合领域标签（如 人工智能/计算机视觉 → 人工智能）的别名表 `DOMAIN_ALIAS` 仍仅在代码中维护。
- **重算时保留人工标记**：仅删除「候选」行；已对接/已排除行通过 upsert 更新分数与理由。

## Excel 导入规则

- **文件发现**：按前缀（`1 *.xlsx` 至 `5 *.xlsx`）匹配，取文件名字尾排序最末一份（日期后缀自然符合）。新快照直接放入目录即可，无需改代码。
- **格式校验**：导入前按文件校验表头列名（见 `ingest/excel_import.py:EXPECTED_HEADERS`）。不符 → 该文件被拒绝并在导入报告中显示明确错误信息；其余文件正常导入。
- **文件 1 格式自动识别**：读取 OLE2 魔数（D0CF11E0）→ xlrd；否则 openpyxl。若 WPS 将文件 1 另存为真 xlsx，导入不会中断。
- **合并逻辑**：文件 2 重复行（同一企业、不同业务线）通过 `normalize.py` 的 `merge_segments()` 合并；文件 2 + 文件 5 通过 `name_norm` upsert 归并，资质去重。文件 1 延续行（公司名留空）自动沿用上一行公司名。
- **计划落地场景关联**：文件 1 的自由文本 `planned_scene` → scenario_id 通过语义相似度挂接（阈值 0.60，见 `engine.py:link_projects_semantic`）。当前 11/17 个项目已关联。

## 如何读取源 Excel 文件

本仓库的 `.xlsx` 文件不能直接用 Read 工具打开——始终用 Python：

- `python3`（3.14，位于 `/usr/local/bin/python3`）已安装 `openpyxl`、`pandas`、`xlrd`。
- **文件 2~6** 为真正的 Excel 2007+ 文件：`openpyxl.load_workbook(...)` 或 `pd.read_excel(...)`。
- **文件 1 是 WPS 保存的旧格式 OLE2/.xls 文档，扩展名错误地标为 `.xlsx`。**openpyxl 打开会报 `File contains no valid workbook part`。必须用 `xlrd.open_workbook(...)` 或 `pd.read_excel(path, engine='xlrd')`。
- WPS Office 是本机上的编辑工具；编辑时尽量保留其原有格式——优先生成新文件而非原地修改原文件。

## 数据模型——6 张文件之间的关系

文件按逻辑顺序编号；文件名后缀为快照日期（文件 1~4 为 20260611，文件 5~6 日期较晚——批次不同，行数据可能不一致）。

| # | 文件（职责） | 行数 | 粒度 / 键 |
|---|---|---|:---:|
| 1 | 场景搭建项目责任清单 — 17 家重点企业项目责任（落地计划、对接部门、跟进人） | 30 | 公司名称 |
| 2 | 场景能力清单 — **企业主表**（113 家去重企业，共 120 行——7 行不同业务线重复，导入时自动合并） | 120 | 公司名称 |
| 3 | 应用场景清单 — 规范的 **"南湾25场景"**（25 个场景：应用领域、拟落地应用区域、牵头部门） | 25 | 应用场景名称 |
| 4 | 街道通用场景项目储备库 — 80 个通用场景参考库（含主要技术、参考案例、潜在企业及产品——该人工标注用作匹配准确率标准答案） | 80 | 场景名称 |
| 5 | 重点企业深度画像 — 深度档案；覆盖文件 2 中 115 家企业中的 113 家（2 家企业仅出现于文件 2） | 115 | 企业名称 |
| 6 | 政府采购中标记录 — 15 家企业可核实中标记录（**未导入**系统；预留后续纳入） | 15 | 企业名称 |

- **文件 1、2、5、6 之间的关联键是企业名**（列名因文件而异：公司名称 / 企业名称）。名称常带括号后缀（如 `（专精特新企业、国高企业）`）或内含换行——关联前须去除 `（…）`/`(…)` 及空白，否则关联将静默丢失。
- 文件 6「是否在南湾25场景有相关项目」一列所指的 25 个场景即**文件 3**。
- 文件 3 与文件 4 仅有少量场景同名：文件 4 是通用池，供抽取新场景使用，而非文件 3 的超集。
- 两套分类法，不可混淆：文件 1~2 的企业「类别」使用圈码桶（②人工智能…⑪生物医药，实际值是不一致的复合串，如 `③节能环保`）；文件 3~4 的「应用领域」使用领域标签（智慧交通、低空经济……，其中文件 4 比文件 3 更细粒度）。

## 雷区

- 文件 1：第 1 行为合并标题行，真正的表头在第 2 行，数据从第 3 行开始。经 xlrd 读取后，数值单元格（序号）会变成浮点数（`1.0`）。
- 单元格常含多行文本（`\n`）——比对或导出前务必规整。
- 文件 5 和文件 6 使用通用 sheet 名 `Sheet1`；文件 1 的数据在 `Sheet2` 上。
- 以上行数为最近一次导入对账数据（2026-07-27，系统实际数据）。源文件更新后，可通过 `python -m ingest.excel_import` 重新对账。
