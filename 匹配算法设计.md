# 南湾街道政企供需匹配系统 — 匹配算法设计文档

> 版本：2026-07 · 对应代码：`app/matching/`（engine.py / embed.py / rules.py）
> 读者：街道业务口与后续维护开发人员。本文解释"系统为什么推荐这家企业"。

---

## 1. 系统要解决的问题

把街道的两类资产双向对接：

- **供给侧**：115 家重点企业（能力清单 + 深度画像合并的一企一档）
- **需求侧**：25 个"南湾25场景" + 80 个储备场景

匹配引擎回答两个方向的问题：**给场景找候选企业**（匹配工作台主流程）、**给企业找机会场景**。每条推荐都必须附带**可向领导解释的理由**，这是政务场景的硬约束。

## 2. 总体结构：双路融合

```
总分 = 0.7 × 语义分（BGE 向量相似度）
     + 0.3 × 规则分（0.5 × 类别映射 + 0.5 × 领域词命中）
```

| | 语义路（主力，70%） | 规则路（锚定+解释，30%） |
|---|---|---|
| 擅长 | 无共同关键词但语义相关的配对（"高空抛物识别"↔"安全隐患整治"） | 结构化先验知识（类别、领域词），结果确定、可解释 |
| 短板 | 黑盒，短文本偶有噪声 | 词库覆盖不到就漏配 |

设计意图：语义分承担"找得全"，规则分承担"说得清"并防止语义噪声把明显对口的企业埋掉。

## 3. 语义路：BGE 中文向量模型

**模型**：`BAAI/bge-small-zh-v1.5`（北京智源研究院开源，C-MTEB 中文检索榜前列）。
选型理由：① 检索任务形状对口（短查询→长文档检索，非句对判重）；② ~100MB、CPU 可跑；
③ 开源可完全离线，政务/企业数据不出本机；④ Intel Mac 兼容（torch 2.2.2 + python3.11）。
模型经 ModelScope 下载（HuggingFace 本机不可达），权重存于 `app/data/models/`。

**匹配文本构造**（只喂与"能力对口"相关的字段，其余是展示元数据）：

- 企业侧 = 核心定位 + 简介 + 场景能力 + 核心产品 + 核心技术 + 可应用落地场景（≤1024 字）
- 场景侧 = 名称 + 简介 + 应用领域 + 主要技术（≤512 字）

**非对称编码**：本任务是检索（场景=query，企业=passage）。按 BGE 官方建议，场景侧文本前加指令前缀
「为这个句子生成表示以用于检索相关文章：」，企业侧直接编码。向量 L2 归一化后，
**余弦相似度 = 矩阵点积**，115 企业 × 105 场景 = 12,075 对一次矩阵乘法算完（CPU 秒级）。

**存储与版本**：向量以 float32 BLOB 存 `embeddings` 表并记录 `model_ver`，换模型后全量重算即可。

## 4. 规则路：类别映射 + 领域词库

### 4.1 类别映射（占规则分 50%）

源数据有两套互不相通的分类法：企业侧圈码类别（②人工智能…⑪生物医药）、场景侧应用领域
（智慧交通、低空经济…）。`CATEGORY_DOMAIN_MAP` 是两套体系间的人工桥，如
`通信技术 → 智慧园区/智慧安防/智慧交通/数字政府/智慧治理`。

注意：圈码会复用（③既是无人机又是节能环保），映射建在"去圈码后的文本桶"上而非圈码本身。

### 4.2 领域词库（占规则分 50%，命中 3 词即满分）

19 个领域 × 5~12 个**专属证据词**（`rules.py: DOMAIN_LEXICON`）：

```
智慧交通: [交通, 违停, 车流, 停车, 信号灯, 斑马线, 拥堵, 车路协同, ...]
智慧安防: [安防, 视频监控, 摄像, 人脸, 车牌, 门禁, 高空抛物, ...]
人工智能: [AI, 大模型, 机器视觉, 图像识别, 深度学习, 算法, ...]
...（完整表见 rules.py 或系统"词库"管理页）
```

**打分流程**：场景先做领域检测（① 官方 domain 字段直查/别名表归并；② 场景文本命中领域词自动打标，
可多标签）→ 取这些领域的词单 → 到企业文本中查命中 → `min(1, 命中数/3)`。

**策展三原则**（业务口维护词库时的尺子）：

1. **一词一证**：词条必须是"出现在企业文本里即指向本领域"的专属证据（高空抛物→安防 ✓）；
2. **泛词禁入**：监测/治理/智能/自动化/传感器 类词不构成任何领域的专属证据，
   一律不入库（实测这类词使命中近乎随机、Top5 不升反降）；跨领域相关性由类别映射与语义路承担；
3. **允许边界模糊**：一个词归错格子代价很小（只影响相邻领域），场景多打标签代价近似零
   （"作业"误标场景为教育领域，但教育词在企业侧极少命中——漏标才是真损失，故保留）。

**英文缩写边界守卫**：AI/AR/VR/AGV/MEMS 等纯英文词要求非字母边界，避免 `AI` 命中 `OPENAI`。

**在线维护**：词条存 `lexicon_terms` 表，管理页浏览器增删、即改即生效（无需改代码/重启）；
表为空时回退 `rules.py` 内置默认词库（"恢复出厂"）。类别映射与别名映射属结构配置，仍在代码中维护。

## 5. 融合、排序与可解释性

- 总分 ≥ **0.25** 且每个场景最多保留 **30** 条候选（阈值/上限见 `config.py`）。
- 每条推荐生成理由串，三证据并列，例：

> `类别映射：通信技术→智慧交通；领域词：交通、信号灯、斑马线；语义相似度 0.62`

## 6. 预计算与状态保持

- **全量预计算**：匹配结果物化到 `matches` 表，页面查询即索引 SQL；档案编辑/审核通过/
  Excel 重导后自动全量重算（220 条文本编码+矩阵乘+写库共数秒，规模上无需真增量）。
- **人工标记存活**：重算只删"候选"状态的行；"已对接/已排除"行通过
  `INSERT ... ON CONFLICT DO UPDATE` 仅刷新分数与理由——机器计算永远不推翻人的决定。
- **优雅降级**：模型缺失时语义分记 NULL、总分退化为纯规则分，页面顶部提示，系统其余功能不受影响。

## 7. 项目 ↔ 场景语义关联

责任清单（文件1）的"计划落地应用场景"是自由文本（如"沙湾河""布沙路"），字符串匹配几乎关联不上
场景库。改用 BGE 向量：planned_scene 作 query 与全部场景比对，相似度 ≥ **0.60** 才挂接。
阈值经质量校准：0.55 时出现误配（安全监管→量子通信），0.60 下 11 条链接全部准确
（布沙路→道路交通隐患整治、国际综合物流枢纽→智能物流枢纽中心等）。

## 8. 效果评估（词库版本演进的依据）

**标准答案**：储备库 69 个场景的人工标注"潜在企业"，其中 91 对企业在库。
**指标**：Top1/Top5/Top10 命中率、标注企业平均排名（全量 115 家排名，非 Top30 截断口径）。

| 方案 | Top1 | Top5 | Top10 | 平均排名 | 结论 |
|---|---|---|---|---|---|
| v1 平铺词库（91 通用词） | 9 | 38 | 51 | 15.5 | 基线 |
| v1+8 个泛词（监测/治理等） | 9 | 36 ↓ | 51 | 15.1 | **泛词有害，否决** |
| 领域词库 2 词饱和 | 5 ↓ | 38 | 58 ↑ | 14.5 | 召回最好但 Top1 腰斩 |
| **领域词库 3 词饱和（采用）** | 8 | 38 | 56 ↑ | 14.8 | 均衡，召回+5，Top1 基本不降 |

说明：91 对样本下 ±2~3 属噪声；Top10 召回提升与理由可解释性（"关键词：AI、监测" →
"领域词：交通、信号灯、斑马线"）是稳定收益。冒烟测试按"标注企业全部进 Top10 且至少一家进 Top3"
断言，而不是锁定某个固定名次（系统曾把标注漏掉的维的美光电（LED交通信号灯）排到第一——
合理的新发现，不应判为回归）。

## 9. 已知局限与后续方向

1. **0.7/0.3 权重与 0.25 阈值为经验值**——数据量增大后可按第 8 节方法网格搜索调优；
2. **BGE-small 短文本有噪声**——可换 bge-base/large（`embeddings.model_ver` 已留好版本位）；
3. **类别映射/领域词库是人工先验**——随新业务线出现需维护（系统内已有词库页与评估方法）；
4. **推荐理由为模板拼装**——如需更自然的说明文字，可在配置国产大模型 API 后生成
   （当前架构已预留位置，但政务数据合规角度默认不启用）；
5. 不建议上 jieba+TF-IDF/BM25 词频路：220 篇语料太薄，词不匹配问题已由语义路更好地解决。

## 10. 参数速查

| 参数 | 值 | 位置 |
|---|---|---|
| 语义/规则权重 | 0.7 / 0.3 | `config.py: SEMANTIC_WEIGHT / RULE_WEIGHT` |
| 候选门槛 / 每场景上限 | 0.25 / 30 | `config.py: MATCH_MIN_SCORE / MATCH_TOP_K` |
| 领域词饱和点 | 3 词 | `rules.py: DOMAIN_HIT_SATURATE` |
| 项目-场景关联阈值 | 0.60 | `engine.py: link_projects_semantic` |
| BGE 查询指令前缀 | 「为这个句子生成表示以用于检索相关文章：」 | `config.py: QUERY_INSTRUCTION` |
| 类别映射 / 领域词库 / 别名 | — | `rules.py`（词库同时支持数据库在线覆盖） |
