# 南湾街道政企供需匹配系统 — 匹配算法设计文档

> 版本：2026-07-31 · 对应代码：`app/matching/`
> 涵盖三个引擎：场景↔企业匹配、政策↔企业匹配、外迁预警打分

---

## 一、场景 ↔ 企业双向匹配

> 代码：`engine.py` + `embed.py` + `rules.py`

系统把街道的两类资产双向对接——**供给侧** 115 家重点企业（能力清单 + 深度画像合并的一企一档），**需求侧** 25 个"南湾 25 场景" + 80 个储备场景。匹配引擎回答两个方向：**给场景找候选企业**（匹配工作台主流程）、**给企业找机会场景**。每条推荐附带可解释理由。

### 1. 总体结构：双路融合

```
总分 = 0.7 × 语义分（BGE 向量相似度）
     + 0.3 × 规则分（0.5 × 类别映射 + 0.5 × 领域词命中）
```

| | 语义路（主力，70%） | 规则路（锚定 + 解释，30%） |
|---|---|---|
| 擅长 | 无共同关键词但语义相关的配对 | 结构化先验知识，结果确定、可解释 |
| 短板 | 黑盒，短文本偶有噪声 | 词库覆盖不到就漏配 |

设计意图：语义分承担"找得全"，规则分承担"说得清"并防止语义噪声把明显对口的企业埋掉。

### 2. 语义路：BGE 中文向量模型

**模型**：`BAAI/bge-small-zh-v1.5`（北京智源研究院开源，C-MTEB 中文检索榜前列），~100MB、CPU 可跑、开源可完全离线。模型经 ModelScope 下载，权重存于 `app/data/models/`。

**匹配文本构造**：
- 企业侧 = 核心定位 + 简介 + 场景能力 + 核心产品 + 核心技术 + 可应用落地场景（≤1024 字）
- 场景侧 = 名称 + 简介 + 应用领域 + 主要技术（≤512 字）

**非对称编码**：场景 = query，企业 = passage。场景侧文本前加指令前缀「为这个句子生成表示以用于检索相关文章：」，企业侧直接编码。向量 L2 归一化后余弦相似度 = 矩阵点积，115 企业 × 105 场景一次矩阵乘法算完（CPU 秒级）。向量以 float32 BLOB 存 `embeddings` 表并记录 `model_ver`。

### 3. 规则路：类别映射 + 领域词库

**类别映射**（占规则分 50%）：企业侧圈码类别（②人工智能…⑪生物医药）与场景侧应用领域（智慧交通、低空经济…）是两套互不相通的分类法。`CATEGORY_DOMAIN_MAP` 是两套体系间的人工桥，如 `通信技术 → 智慧园区/智慧安防/智慧交通/数字政府/智慧治理`。映射建在"去圈码后的文本桶"上而非圈码本身。

**领域词库**（占规则分 50%，命中 3 词即满分）：19 个领域 × 5~12 个专属证据词（如 智慧安防: [安防, 视频监控, 摄像, 人脸, 车牌, 门禁, 高空抛物, …]）。词条策展三原则：必须是专属证据词、泛词禁入（监测/治理/智能等不构成任何领域专属证据）、允许边界模糊。

领域词库存于两处：`rules.py:DOMAIN_LEXICON`（出厂默认）和数据库 `lexicon_terms` 表（管理员在线增删，有数据时优先于代码默认值，空表回退代码值，修改即时生效无需重启）。

**英文缩写边界守卫**：AI/AR/VR/AGV/MEMS 等纯英文词要求 ASCII 词边界，避免 `AI` 命中 `OPENAI`。

### 4. 融合、排序与可解释性

- 总分 ≥ 0.25 且每场景最多保留 30 条候选
- 推荐理由三证据并列，如：`类别映射：通信技术→智慧交通；领域词：交通、信号灯、斑马线；语义相似度 0.62`
- 预计算后物化到 `matches` 表，页面查询即索引 SQL
- 重算仅删"候选"状态行，"已对接/已排除"通过 `ON CONFLICT DO UPDATE` 保留

### 5. 项目 ↔ 场景语义关联

责任清单（文件 1）的"计划落地应用场景"为自由文本，用 BGE 向量与全部场景比对，相似度 ≥ 0.60 挂接。

### 6. 参数速查

| 参数 | 值 | 位置 |
|---|---|---|
| 语义/规则权重 | 0.7 / 0.3 | `config.py` |
| 候选门槛 / 上限 | 0.25 / 30 | `config.py` |
| 领域词饱和点 | 3 词 | `rules.py` |
| 项目-场景关联阈值 | 0.60 | `engine.py` |

---

## 二、政策 ↔ 企业双向匹配

> 代码：`policy_engine.py`

与场景匹配不同，政策匹配**不用语义模型、不设权重、不打分数**。政策申报条件是硬性门槛——企业要么满足、要么不满足，不存在"差不多满足"。

### 1. 核心逻辑：条款逐条布尔判定

每条政策的申报条件拆为多条条款（5 种类型），对每家企业逐条判定，结果三选一：

| 判定结果 | 含义 | 影响 |
|----------|------|------|
| 满足 | 企业数据明确满足该条款 | 该条款通过 |
| 不满足 | 企业数据明确不满足该条款 | **企业直接排除**，不论其他条款是否满足 |
| 数据不足 | 企业缺少该条款所需的数据字段 | 保留为候选，在结果中说明缺什么数据 |

**全部条款不可判定 → 不列入候选**（等于完全不知道这家企业符不符合，不瞎推荐）。

### 2. 五类条款及其判定逻辑

| 条款类型 | 数据源 | 判定方式 |
|----------|--------|----------|
| **资质** | `enterprises.qualifications` | 从条款内容提取资质关键词 → 生成多候选变体（去前缀、去括号、去级别、拆分尾缀）→ 逐变体在企业资质文本中搜索子串。任一命中即通过 |
| **行业** | `enterprises.category_bucket` | 从条款内容提取行业关键词 → 与企业行业桶（如"人工智能/无人机/节能环保"）做集合交集 |
| **规模** | `enterprises.registered_capital` / `employee_count` | 正则提取数值阈值（"注册资本≥X 万"、"员工人数≥X 人"）→ 数值比较 |
| **地域** | 无数据 | **一律默认通过**——所有企业均属南湾街道辖区 |
| **信用** | `enterprises.biz_anomaly` | 检查经营异常字段，无异常或数据为空时通过 |

### 3. 资质关键词变体生成（核心技巧）

政府资质名称在实际数据中写法多样（"国家级专精特新小巨人企业" vs ""小巨人"企业"），不能用精确匹配。生成多条候选变体依次尝试：

- 原始关键词（如"国家级专精特新小巨人企业"）
- 去括号内容（如去掉"（证书有效期内）"）
- 去级别前缀（"国家级"→"专精特新小巨人企业"）
- 去"企业"尾缀 → "专精特新小巨人"
- 从中间分隔点切开取后半段（"特新"→"小巨人企业"、"小巨人"）

任一变体在企业资质文本中命中即返回"满足"。此方法将资质认定类匹配从 0 条提升到 75 条。

### 4. 候选排序与人工标记保持

候选企业按满足条款数降序排列（无权重、不打总分）。重算时仅删 `status='候选'` 的行——已对接/已排除的人工标记通过 `ON CONFLICT DO UPDATE` 保留。

---

## 三、外迁预警

> 代码：`relocation_rules.py`

基于龙岗区外迁预警清单的企业多维度外迁风险自动打分与分级。权重从 605 条标注数据反向推导（高=2、中=1、低=0 加权平均），企业级准确率 79.4%。

### 1. 三特征打分模型

```
record_score = 预警类型基础分 × 租赁面积调节 × 行业系数调节
企业总分 = Σ(该企业所有预警记录的分数)
```

### 2. 特征详解

**特征一：预警类型基础分**（10 种类型，从数据标注反推）

| 预警类型 | 基础分 | 依据 |
|----------|:--:|------|
| 限制高消费 | 10 | 125/125 全为高风险 |
| 异地购地 | 10 | 6/6 全为高风险 |
| 失信执行 | 10 | 25/25 全为高风险 |
| 区内租赁临期 | 9 | 36 高 / 4 中 / 2 低，期望分 1.81 |
| 舆情动态 | 5.5 | 116 高 / 14 中 / 94 低，期望分 1.10 |
| 经营异常 | 5 | 5 条全为中风险 |
| 行政处罚 | 2.6 | 20 中 / 18 低 |
| 异地分支 | 2.6 | 40 条全为低风险（高新技术企业开分支机构是正常扩张） |
| 异地投资 | 2.6 | 96 条全为低风险（同上） |
| 公开市场融资 | 2.6 | 4 条全为低风险 |

**特征二：租赁面积占比调节**（仅`区内租赁临期`类型生效）

从预警信息文本中正则提取`占其在龙岗总租赁面积的 X%`：

| 面积占比 | 调节 | 逻辑 |
|----------|:--:|------|
| ≥80% | +1.5 | 在龙岗几乎全部空间都受影响 |
| 50-79% | 不变 | 部分受影响 |
| <50% | -1.5 | 仅小部分空间，影响有限 |

**特征三：行业系数**（仅`舆情动态`和`区内租赁临期`生效——法律类预警如限制高消费不受行业影响）

| 行业 | 系数 | 依据 |
|------|:--:|------|
| 制造业 | ×1.3 | 高风险率 56%，重资产外迁成本高 |
| 居民服务业 | ×1.3 | 高风险率 75% |
| 建筑业 | ×1.2 | 高风险率 62% |
| 交通仓储邮政 | ×1.1 | 高风险率 67% |
| 批发零售 | ×1.0 | 基准 |
| 科技服务 | ×0.8 | 高风险率 34%，服务型企业流动性高 |
| 软件信息技术 | ×0.7 | 高风险率 25%，驻外分支是常态 |
| 水利环境 | ×0.7 | 高风险率 6% |
| 其他 | ×1.0 | 默认 |

### 3. 分级阈值

```
总分 ≥ 10   → 挽留级  （一条限制高消费就触发，需组建专项服务小组）
总分 5-9.9  → 警示级  （两条舆情、或一条租赁临期，需加大走访频次）
总分 2.6-4.9 → 关注级  （一条异地投资/行政处罚，定期跟踪即可）
总分 < 2.6  → 无风险
```

### 4. 无法使用的特征及原因

| 特征 | 原因 |
|------|------|
| 产能流向 | 76% 数据为"未知-未知-未知"，且剩余样本被预警类型覆盖 |
| 预警时间 | 数据为单次快照，无时间跨度，无法做衰减 |
| 关注线索类型 | 与预警类型 1:1 映射，无额外信息 |
