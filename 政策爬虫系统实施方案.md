# 政策爬虫系统实施步骤

## 实测结果（2026-07-29）

4 站全部可达，无 robots.txt，编码均为 UTF-8：

| key | 站点 | 首页 | 政策链接数 |
|-----|------|:--:|:--:|
| sz_gxj | 深圳市工信局 | http://gxj.sz.gov.cn/ | 16 |
| lg_gov | 龙岗区政府 | http://www.lg.gov.cn/ | 13 |
| sz_kcw | 深圳市科创委 | http://stic.sz.gov.cn/ | 18 |
| gd_gxt | 广东省工信厅 | http://gdii.gd.gov.cn/ | 14 |

---

## 第一步：数据库（2 个文件，约 20 行）

### 1.1 `schema.sql` 追加

政策表后面加一张 crawl_log：

```sql
CREATE TABLE IF NOT EXISTS crawl_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  target_key TEXT NOT NULL,       -- sz_gxj / lg_gov / sz_kcw / gd_gxt
  target_name TEXT NOT NULL,      -- 站点中文名
  urls_scanned INTEGER DEFAULT 0,
  urls_new INTEGER DEFAULT 0,
  drafts_created INTEGER DEFAULT 0,
  errors TEXT,                    -- JSON 数组
  started_at TEXT,
  finished_at TEXT,
  created_at TEXT DEFAULT (datetime('now','localtime'))
);
```

policies 表追加 3 列（迁移 DDL 写在 sql 文件注释里即可，实际由 db.py 执行）：

```sql
-- ALTER TABLE policies ADD COLUMN crawled_at TEXT;
-- ALTER TABLE policies ADD COLUMN crawl_source TEXT;
-- ALTER TABLE policies ADD COLUMN content_hash TEXT;
```

### 1.2 `db.py` init_db() 追加

跟在 enterprises 字段迁移后面，try/except 安全迁移：

```python
for col, typ in [('crawled_at', 'TEXT'), ('crawl_source', 'TEXT'), ('content_hash', 'TEXT')]:
    try:
        conn.execute(f'ALTER TABLE policies ADD COLUMN {col} {typ}')
    except Exception:
        pass
```

---

## 第二步：配置 + 依赖（2 个文件，约 10 行）

### 2.1 `config.py`

```python
# 政策爬虫
CRAWL_ENABLED = False        # 默认关闭，需要时手动开启
CRAWL_SCHEDULE_HOUR = 8      # 每天 8:00
CRAWL_RATE_LIMIT = 3          # 请求间隔（秒）
```

### 2.2 `requirements.txt`

```
beautifulsoup4>=4.12
lxml>=5.1
requests>=2.31
apscheduler>=3.10
```

安装：`.venv/bin/pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

---

## 第三步：爬虫核心模块（`app/crawler/`，5 个文件）

新建目录 `app/crawler/`，放 5 个文件：

### 3.1 `__init__.py` — 入口

只有一个公开函数 `run_crawl(conn, target_keys=None) -> dict`：

1. 如果 `target_keys` 为空 → 遍历全部 4 站
2. 对每个站点：
   - 记录 `started_at`
   - 创建 Fetcher 实例
   - 调 `fetcher.crawl_list()` 获取候选详情页 URL 列表
   - 对每个候选 URL：调 `deduplicate.check()` → 调 `fetcher.fetch_detail()` → 调 `extractor.extract()` → `INSERT` 入库
   - 记录 `finished_at` 和统计数，写入 `crawl_log`
3. 返回 `{'sz_gxj': {scanned: N, new: N, drafts: N}, ...}`

### 3.2 `targets.py` — 站点配置

```python
CRAWL_TARGETS = {
    'sz_gxj': {
        'name': '深圳市工业和信息化局',
        'level': '市级',
        'home_url': 'http://gxj.sz.gov.cn/',
        'encoding': 'utf-8',
        # 列表页提取规则
        'link_filter': lambda a: any(kw in (a.get_text() or '') for kw in
            ['申报', '通知', '公示', '资金', '补贴', '认定', '指南', '项目']),
        # 详情页选择器
        'selectors': {
            'title': 'h1, .article-title, #title',
            'date': '.article-info, .info, #pubtime, .time',
            'content': '#zoom, .article-content, .TRS_Editor, .content, .article-body',
        },
    },
    'lg_gov': {
        'name': '龙岗区人民政府',
        'level': '区级',
        'home_url': 'http://www.lg.gov.cn/',
        'encoding': 'utf-8',
        # 龙岗区首页杂（教师招聘等），额外要求命中"工信/产业/科技/经济"
        'link_filter': lambda a: (
            any(kw in (a.get_text() or '') for kw in ['申报', '通知', '公示', '资金', '补贴', '认定', '指南'])
            and any(kw in (a.get_text() or '') for kw in ['工信', '产业', '科技', '经济', '工业', '制造', '企业'])
        ),
        'selectors': {同上},
    },
    'sz_kcw': {
        'name': '深圳市科技创新局',
        'level': '市级',
        'home_url': 'http://stic.sz.gov.cn/',
        'encoding': 'utf-8',
        'link_filter': lambda a: any(kw in (a.get_text() or '') for kw in
            ['申报', '通知', '公示', '指南', '征集', '项目']),
        'selectors': {同上},
    },
    'gd_gxt': {
        'name': '广东省工业和信息化厅',
        'level': '省级',
        'home_url': 'http://gdii.gd.gov.cn/',
        'encoding': 'utf-8',
        'link_filter': lambda a: any(kw in (a.get_text() or '') for kw in
            ['申报', '通知', '公示', '资金', '补贴', '认定', '指南', '项目']),
        'selectors': {同上},
    },
}
```

每个站的 `link_filter` 是 Python lambda，在 fetcher 中用来过滤列表页上的 `<a>` 标签。

### 3.3 `fetcher.py` — HTTP 客户端

```python
class Fetcher:
    def __init__(self, target_config):
        self.config = target_config
        self.session = requests.Session()
        self.session.headers['User-Agent'] = 'Mozilla/5.0 ...'
        self.last_request = 0  # 限速用

    def _rate_limit(self):
        """两次请求间隔 >= CRAWL_RATE_LIMIT 秒"""
        ...

    def _get(self, url):
        """GET + rate_limit + encoding 处理"""
        self._rate_limit()
        r = self.session.get(url, timeout=15)
        r.encoding = self.config['encoding']  # utf-8
        return r

    def crawl_list(self):
        """
        1. GET 首页
        2. BeautifulSoup 解析
        3. 找出所有 <a href> 标签
        4. 用 target.link_filter 过滤
        5. 补全相对 URL（urljoin）
        6. 返回候选 URL 列表（去重，最多 50 条）
        """

    def fetch_detail(self, url):
        """
        1. GET 详情页
        2. 用 target.selectors 取 title/content/date
        3. 返回 {'title': ..., 'content_html': ..., 'date': ..., 'url': ...}
        """
```

三个关键细节：
- **相对 URL 补全**：`from urllib.parse import urljoin` → `urljoin(home_url, href)`
- **限速**：每次 GET 前用 `time.sleep()` 保证间隔（不依赖 APScheduler 做限速）
- **编码**：直接用站点配置的 encoding，不做自动检测（四个站实测都是 UTF-8）

### 3.4 `extractor.py` — 内容提取

```python
def extract(html, url, target_config):
    """
    输入：详情页 HTML + URL + 站点配置
    输出：{'name': ..., 'level': ..., 'category': ..., ...,
           'clauses': [{type, content}, ...],
           'source_url': url, 'content_hash': sha256(text)}

    分两步：
    """

def _extract_meta(soup, target_config):
    """
    规则提取（不调 LLM）：
    - name:     soup.select_one(selectors['title']).get_text()  → 清理站点后缀
    - level:    target_config['level']（按站点硬编码）
    - category: 标题命中"科技/创新/研发"→科技创新，命中"产业/制造/工业"→产业扶持，
                命中"财税/税收/贷款/贴息"→财税金融，命中"认定/资质/专精特新"→资质认定
                默认→产业扶持
    - support_type: 正文命中"奖励/资助/补贴"→资金奖励，"税收/优惠/减免"→税收优惠，
                    "贷款/贴息"→贷款贴息，"租金"→租金补贴，默认→资金奖励
    - deadline:  正则提取（YYYY年MM月DD日 / YYYY-MM-DD / YYYY/MM/DD）
    - amount_text: 正则提取"最高 N 万元" / "给予 N 万元" 等
    - source_url: 传入的 url 参数
    """

def _extract_clauses(text):
    """
    调 DeepSeek API：
    - prompt 把正文截到 4000 字（避免 token 超限）
    - 返回 JSON 数组 [{clause_type, content}, ...]
    - 失败/超时 → 返回 []（降级为空，管理员手补条款）
    """
```

元数据全部规则提取，不耗 LLM 配额。只有条款（申报条件）调 LLM，且失败就空着——管理员审核时手补。

### 3.5 `deduplicate.py` — 去重

```python
def is_duplicate(conn, url, title, content_text):
    """
    三级去重：

    1. URL 精确匹配
       SELECT id FROM policies WHERE source_url=?
       → 有结果 → True（已存在，跳过）

    2. 名称模糊匹配
       - 清洗标题和库内政策名（去"关于开展""的通知""的申报指南""的通知"）
       - SELECT id FROM policies WHERE name LIKE '%cleaned_title%'
       → 有结果 → True

    3. content_hash 匹配
       - SHA256(content_text)
       - SELECT id FROM policies WHERE content_hash=?
       → 有结果 → True

    全部不命中 → False（可以入库）
    """
```

---

## 第四步：Web 集成（4 个文件修改 + 1 个新页面）

### 4.1 `routers/policy.py` — 3 个新路由

```python
# 1. 抓取状态页面
@router.get('/policies/crawl')
def crawl_status(request: Request):
    # admin only
    # 查询 crawl_log 最近 20 条
    # 查询 status='草稿' 的政策数
    # 渲染 crawl_status.html

# 2. 手动触发抓取
@router.post('/policies/crawl/trigger')
def crawl_trigger(request: Request):
    # admin only
    # 调 crawler.run_crawl(conn) → stats
    # 重定向回 /gov/policies/crawl

# 3. 发布草稿
@router.post('/policies/{pid}/publish')
def policy_publish(request: Request, pid: int):
    # admin only
    # UPDATE policies SET status='已发布' WHERE id=?
    # 调 policy_engine.recompute_policy_matches(conn, pid)
    # 重定向回 /gov/policies/{pid}
```

列表页（`def policies(...)`）的 status 筛选加一个 `<option value="草稿">草稿</option>`。

### 4.2 `templates/gov/policies.html`

- 状态筛选 dropdown 追加 `<option value="草稿">草稿</option>`
- 表格行中，`status == '草稿'` 显示琥珀色 badge
- 草稿行操作列加「发布」按钮：`POST /gov/policies/{id}/publish`

### 4.3 `templates/gov/policy_edit.html`

- 当 `p.status == '草稿'` 时，表单上方显示蓝色提示条：
  > 「此政策由系统自动抓取生成，请逐项审核后发布」
- 提交按钮旁边加一个「保存并发布」按钮（提交到 `/gov/policies/{id}/publish`）

### 4.4 `templates/gov/crawl_status.html`（新建）

继续用现有模板风格（card + toolbar + table）：

```
┌─ 政策抓取 ─────────────────────────────────────┐
│  [手动触发抓取]  上次运行：2026-07-29 08:00     │
├─────────────────────────────────────────────────┤
│  抓取历史                                      │
│  站点 │ 扫描 │ 新增 │ 草稿 │ 耗时 │ 时间       │
│  工信局  45     3      2      12s   07-29 08:00 │
│  龙岗区  38     1      1      8s    07-29 08:00 │
│  ...                                           │
├─────────────────────────────────────────────────┤
│  待审核草稿（N 条）                              │
│  政策名称 │ 站点 │ 抓取时间 │ 操作              │
│  ...     │ ...  │ ...     │ [编辑] [发布] [删除]│
└─────────────────────────────────────────────────┘
```

### 4.5 `templates/base.html`

导航栏，「政策库」旁边加一个 admin only 的「政策抓取」入口：
```html
{% if user.role=='admin' %}
<a href="/gov/policies/crawl">政策抓取</a>
{% endif %}
```

---

## 第五步：调度（2 个文件）

### 5.1 `crawler/scheduler.py`

```python
from apscheduler.schedulers.background import BackgroundScheduler
import config

scheduler = BackgroundScheduler()

def start():
    if not config.CRAWL_ENABLED:
        return
    from crawler import run_crawl
    import db
    scheduler.add_job(
        lambda: _crawl_wrapper(run_crawl),
        'cron',
        hour=config.CRAWL_SCHEDULE_HOUR,
        minute=0,
        id='policy_crawl',
    )
    scheduler.start()

def stop():
    scheduler.shutdown(wait=False)

def _crawl_wrapper(run_crawl):
    with db.get_db() as conn:
        run_crawl(conn)
```

### 5.2 `run.py` — lifespan 中启停

```python
from crawler import scheduler as crawl_scheduler

@asynccontextmanager
async def lifespan(app):
    db.init_db()
    crawl_scheduler.start()
    yield
    crawl_scheduler.stop()
```

### 5.3 `scripts/crawl.py` — CLI 入口（可选，不依赖 Web）

```python
"""手动运行爬虫：python scripts/crawl.py [--target sz_gxj]"""
import sys, db
from crawler import run_crawl

db.init_db()
with db.get_db() as conn:
    target = sys.argv[2] if len(sys.argv) > 2 and sys.argv[1] == '--target' else None
    targets = [target] if target else None
    stats = run_crawl(conn, targets)
    for k, v in stats.items():
        print(f'{k}: scanned={v["scanned"]} new={v["new"]} drafts={v["drafts"]}')
```

---

## 第六步：验证

1. `python scripts/crawl.py --target sz_gxj` → 看输出：scanned/new/drafts 数量
2. 打开 http://127.0.0.1:8000/gov/policies → 筛选「草稿」→ 新入库的草稿
3. 点进一条草稿 → 看字段自动填充质量 → 调整条款 → 点击「保存并发布」
4. 刷新政策详情 → 匹配结果已生成
5. 打开 http://127.0.0.1:8000/gov/policies/crawl → 看抓取历史记录

---

## 涉及的潜在风险

1. **政府网站改版** → CSS 选择器失效 → 某站返回 scanned=0，不影响其他站
2. **DeepSeek API 不可用** → 条款返回空数组 []，草稿照常创建，管理员手补条款
3. **列表页无新链接** → scanned=N, new=0, drafts=0，正常情况
4. **详情页是 PDF** → fetcher 检测 Content-Type，PDF 直接跳过
